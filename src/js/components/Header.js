import React from 'react';

import AppsIcon from '@material-ui/icons/Apps';

const Header = () => (
  <header className="header">
    <AppsIcon />
    <p className="brand-text">Gallery</p>
  </header>
);

export default Header;
